spring:
  h2:
    console:
      enabled: true
  datasource:
    url: "jdbc:h2:file:C:/Users/izebr/IdeaProjects/shootingplace/src/main/resources/database"
    driverClassName: "org.h2.Driver"
    username: sa
    password:

  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"
    hibernate:
      ddl-auto: update